# Public base URL of the SRK Fragrance authentication API. Never put OAuth client
# secrets in Vite environment variables; they must remain on the server.
VITE_AUTH_API_BASE_URL=https://api.example.com
